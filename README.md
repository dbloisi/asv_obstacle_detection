## asv_obstacle_detection
obstacle detection in images from an autonomous surface vessel

# Building
mkdir build
cd build
cmake ../
make

# Running
From build diretory launch the program with the following command:
./obstacle
